def main = "edu.gvsu.cis.campbjos.connectfour.ConnectFourAI"

jar {
    manifest {
        attributes('Main-Class': main)
    }
    // Zip up external dependencies
    from {
        (configurations.runtime).collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
}